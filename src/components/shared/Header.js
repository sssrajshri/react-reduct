import React from 'react';
import { Container, Row, Col } from 'react-bootstrap';

const Header = () => (
  <Row>
    <header>
      <Container>
        <Row>
          <Col>React-Redux Hello world!!</Col>
        </Row>
      </Container>
    </header>
  </Row>
);

export default Header;
